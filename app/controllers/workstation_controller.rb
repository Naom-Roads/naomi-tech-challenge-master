class WorkstationController < ApplicationController
  # TODO a rake task to backfill the workstation names instead of creating a function within show.
  # TODO add table to schema file
  # TODO Add Routes and References and

  def index

  end

  #
  # def show
  #   @machine_one = Metric.first #testing
  #   @metric = Metric.find(name: metric_params[:name]).or(Metric.find(machine_id: metric_params[:machine_id]))
  #   @name = metric.name
  #
  #   end
  end


